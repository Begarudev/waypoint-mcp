{
  "name": "waypoint-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server that gives Claude context for differentiating a lesson to a student's IEP.",
  "type": "module",
  "bin": {
    "waypoint-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch",
    "test": "tsc && node --test dist/_smoke.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
